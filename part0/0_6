```mermaid

sequenceDiagram
    participant browser
    participant server

    browser->>server: POST /exampleapp/new_note
    activate server
    server-->>browser: 201 code
    deactivate server

```
By using "single page app" the page is not reloaded, only the new note is added
